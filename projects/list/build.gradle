subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }

    sourceSets {
        main {
            java.srcDir 'src'
        }
    }
}

dependencies {
    compile project(":projects:version")
    compile ("commons-cli:commons-cli:1.0")
}